# dependencies
node_modules/

# personal / machine-specific hardening helper — NOT part of the reusable plugin
scripts/harden-opencode-v1.jq

# local-only agent guardrails (private to this checkout, never publish)
AGENTS.md

# local reference docs (kept in the checkout, never published)
docs/

# local implementation plan (private to this checkout, never publish)
opencode-permission-reviewer-v1.0.0-plan.md

# audit logs (never commit)
approval-reviewer-audit.jsonl
permission-reviewer-audit.jsonl
*-audit.jsonl

# env / secrets
.env
.env.*
!.env.example

# editor / OS
.DS_Store
.idea/
.vscode/
*.swp
*.log
# Windows alternate data streams (download metadata)
*:Zone.Identifier

# build output (generated by tsup; packaged at release time, not committed)
dist/
coverage/

# reference clones (e.g. codex) if placed inside the worktree
/codex
